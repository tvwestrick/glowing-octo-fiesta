// Two types of operators: unary, binary
// If unary then one branch
// otherwise it has two

// *Employee,

// vtable --> employee or if they're a manager


// Real, complex numbers
// Binary operator (+) works on real, matrices, irrational numbers

// powers, logs, ln(X) 


//array left parentheses
// array right parentheses
// make sure they match
// match them up by index

//left_paren[0] = 0





  
   
